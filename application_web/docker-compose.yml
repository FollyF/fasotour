services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./gestion_cycle/media:/app/gestion_cycle/media
    command: python manage.py runserver 0.0.0.0:8000